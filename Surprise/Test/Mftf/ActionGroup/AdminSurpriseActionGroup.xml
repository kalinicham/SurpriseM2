<?xml version="1.0" encoding="UTF-8"?>

<actionGroups xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/actionGroupSchema.xsd">
    <actionGroup name="fillMainProductSurpriseForm" extends="fillMainProductForm">
        <arguments>
            <argument name="product" defaultValue="simpleSurprise"/>
        </arguments>
        <click selector="{{AdminProductFormSection.enableSurpriseLabel}}" stepKey="clickEnableSurpriseLabelToDisableSurprise" after="fillProductQty"/>
        <remove keyForRemoval="selectStockStatus">selectStockStatus</remove>
        <remove keyForRemoval="selectWeight">selectWeight</remove>
        <remove keyForRemoval="fillProductWeight">fillProductWeight</remove>
    </actionGroup>

    <actionGroup name="seeProductInStoreFrontPageByUrl">
        <arguments>
            <argument name="product" defaultValue="simpleSurprise"/>
        </arguments>
        <scrollTo selector="{{AdminProductSEOSection.sectionHeader}}" x="0" y="-80" stepKey="scrollToAdminProductSEOSection1"/>
        <click selector="{{AdminProductSEOSection.sectionHeader}}" stepKey="clickAdminProductSEOSection1"/>
        <grabValueFrom selector="{{AdminProductSEOSection.urlKeyInput}}" stepKey="grabUrlKey"/>
        <assertNotEmpty actual="$grabUrlKey" stepKey="UrlNotEmpty" />
        <amOnPage url="{{StorefrontHomePage.url}}/$grabUrlKey.html" stepKey="navigateToHomePage" />
        <see selector="{{StorefrontProductInfoMainSection.productName}}" userInput="{{product.name}}" stepKey="seeProductNameInStoreFrontPage"/>
        <see selector="{{StorefrontProductInfoMainSection.productSku}}" userInput="{{product.sku}}" stepKey="seeProductSkuInStoreFrontPage"/>
        <see selector="{{StorefrontProductInfoMainSection.price}}" userInput="{{product.price}}" stepKey="seeProductPriceInStoreFrontPage"/>
    </actionGroup>
</actionGroups>
